#include <stdio.h>

int main()
{
	printf("Hello, world!\n");

	return 0;
}

/*
main함수는 C 언어로 프로그램을 만들었을 때 가장 처음에 실행되는 특별한 함수입니다.
우리는 main 함수를 채워 넣으면서 프로그래밍을 하게 됩니다(만약 소스에서 main 함수가 없으면 컴파일이 되지 않습니다).

즉, C 언어 프로그램은 항상 main 함수가 있어야 하며 main 함수부터 실행된다는 점을 기억하세요.
*/
